import { createClient } from '@supabase/supabase-js'; const key = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY.replace(/[^a-zA-Z0-9.\-_]/g, ''); const url = process.env.NEXT_PUBLIC_SUPABASE_URL.replace(/[^a-zA-Z0-9.:\/-_]/g, ''); const supabase = createClient(url, key); async function test() { const {data, error} = await supabase.from('commerce_products').select('id, commerce_product_images(id, variant_id, storage_path)').eq('editorial_slug', 'matera-de-colgar-cuero'); console.log('ERROR:', error); console.log('DATA:', JSON.stringify(data, null, 2)); } test();
